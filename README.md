# AutoDoctorAze (Frontend, облачная сборка APK)
1) Залей в GitHub весь проект.
2) В Settings → Secrets → Actions добавь `VITE_API_BASE` (URL твоего бэкенда) и при желании `VITE_API_TOKEN`.
3) Во вкладке Actions запусти workflow **Android APK (Capacitor)** и скачай артефакт `app-debug.apk`.